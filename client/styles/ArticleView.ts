import styled from 'styled-components';

const ArticleView = styled.section`
  width: 70%;
  margin: 3rem auto;
  color: black;
  font-size: 1.2rem;

  a {
    color: #511cb1;
  }
`;

export default ArticleView;
