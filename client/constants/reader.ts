import * as colors from './colors';
import { ReaderViewSettings } from '../typings';

export const defaultStyleSettings: ReaderViewSettings = {
  fontSize: '1.2rem',
  backgroundColor: colors.WHITE,
  lineHeight: '1.5',
  width: '70%',
  color: colors.BLACK,
};
