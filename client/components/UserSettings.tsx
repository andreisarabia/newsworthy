import React from 'react';
import styled from 'styled-components';

const SettingsView = styled.main``;

export default class Settings extends React.Component {
  render() {
    return (
      <main>
        <h2>Settings</h2>
      </main>
    );
  }
}
